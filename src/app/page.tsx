export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>aiwrite</h1>
      <p>API is running. See <code>/api/auth/*</code> and <code>/api/files/*</code> endpoints.</p>
    </main>
  );
}
